[package]
name = "ovim"
version = "1.2.7"
edition = "2024"
description = "Oxidized Vim — a snappy, batteries-included editor with terminal and native GUI frontends, Vim keybindings, Lua config, LSP, and AI built in."

[[bin]]
name = "ovim-gui"
path = "src/bin/ovim-gui/main.rs"
required-features = ["gui"]

[[bin]]
name = "key_debug"
path = "src/bin/key_debug.rs"
required-features = ["key-debug"]

[lints]
workspace = true

[dependencies]
ovim-core = { path = "../ovim-core" }
chrono = "0.4"
ratatui = "0.30"
ratatui-image = { version = "11", default-features = false, features = ["crossterm"] }
image = { version = "0.25", default-features = false, features = ["gif", "jpeg", "png", "webp"] }
mathjax-svg-rs = "0.4"
resvg = { version = "0.47", default-features = false }
crossterm = { version = "0.29", features = ["event-stream"] }
futures = "0.3"
anyhow = "1.0"
unicode-width = "0.2"
axum = "0.8"
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"
clap = { version = "4.4", features = ["derive"] }
lsp-types = "0.97"
mlua = { version = "0.9", features = ["lua54", "vendored", "async", "serialize"], optional = true }
reqwest = { version = "0.12.23", features = ["json", "blocking"] }
rand = "0.8"
dirs = "5.0"
prometheus = { version = "0.13", optional = true }
which = "6.0"
shellexpand = "3.1"
tempfile = "3.14"
ignore = "0.4"
unicode-segmentation = "1.12"
open = "5"
flate2 = "1"
tar = "0.4"
zip = "2"
tauri = { version = "2", optional = true, features = ["unstable"] }
# Native folder picker for "Open Project…". Used from Rust only, so no Tauri
# plugin ACL/capability file is needed for the webview to reach it.
rfd = { version = "0.17", optional = true }

[target.'cfg(unix)'.dependencies]
nix = { version = "0.29", features = ["signal", "process"] }
libc = "0.2"

[target.'cfg(windows)'.dependencies]
sysinfo = "0.32"

[features]
default = ["lua", "gui"]
lua = ["mlua", "ovim-core/lua"]
gui = ["dep:tauri", "dep:rfd"]
key-debug = []
metrics = ["prometheus", "ovim-core/metrics"]
# Statically link OpenSSL for portable release binaries
vendored = ["ovim-core/vendored", "reqwest/native-tls-vendored"]

[dev-dependencies]
git2 = "0.19"
tower = { version = "0.5", features = ["util"] }
ropey = "1.6"
tree-sitter = "0.25"
streaming-iterator = "0.1.9"
insta = "1.40"
proptest = "1.5"

[build-dependencies]
tauri-build = { version = "2" }
