☐ Test undo/redo with insert mode changes
☐ Test undo/redo with normal mode commands
☐ Test repeat (.) with various commands (x, dd, insert mode)
☐ Test edge cases: undo empty buffer, redo at end, repeat with no last change
☐ Create Options system (:set number, :set relativenumber, :set tabstop, etc.)
☐ Build status line showing mode, filename, cursor position, modified flag
☐ Add window splitting: horizontal (:sp) and vertical (:vsp) splits
☐ Implement window navigation: Ctrl-W h/j/k/l
☐ Create tab pages system with :tabnew, :tabnext, :tabprev
☐ Build configuration file parser for init.toml or init.lua
☐ Add key mapping system for custom keybinds (nmap, imap, vmap)
☐ Implement syntax highlighting using tree-sitter integration
☐ Add line number display (absolute and relative)
☐ Implement scrolling with viewport optimization for large files
☐ Add UTF-8 and multi-byte character support with proper rendering
☐ Handle different line endings (LF, CRLF) transparently
☐ Implement auto-indent for common programming languages
☐ Add incremental search with live highlighting
☐ Build command history with up/down arrow navigation in command mode
☐ Implement tab completion for commands and file paths
☐ Add error handling and user-friendly error messages
☐ Create comprehensive test suite for buffer operations
☐ Write integration tests for modal editing workflows
☐ Optimize rendering performance with dirty region tracking
☐ Add benchmarks for large file operations
☐ Create documentation: README, architecture guide, and user manual
