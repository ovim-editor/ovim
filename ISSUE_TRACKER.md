# ovim Issue Tracker

| ID | Status | Priority | Complexity | Description |
|----------|---------|----------|------------|-------------|
| OV-00001 | Fixed | HIGH | Medium | [NAV] Ex command `:N` doesn't update cursor position - Fixed: Added line-number command parsing to src/commands.rs |
| OV-00002 | Fixed | HIGH | Medium | [NAV] Normal mode `gg` doesn't update cursor - Fixed: Code analysis confirmed implementation correct; also fixed client error handling in src/client.rs |
| OV-00003 | Pending | MEDIUM | Low | [MCP] `symbols` command fails with "Unknown tool: get_symbols" - should fall back to standard LSP textDocument/documentSymbol if MCP tool unavailable (cli/symbols.rs) |
| OV-00004 | Pending | MEDIUM | Low | [MCP] `diagnostics` command fails with "Unknown tool: get_diagnostics" - should fall back to standard LSP textDocument/publishDiagnostics cache (cli/diagnostics.rs) |
| OV-00005 | Pending | LOW | Low | [STATE] `snapshot` returns null for cursor_line, cursor_col, file fields - state not being serialized correctly (cli/snapshot.rs) |
| OV-00006 | Pending | TRIAGE | Medium | [LSP] `hover` returns null on valid symbol positions - investigate if this is ovim not forwarding request correctly or LSP response handling issue |
| OV-00007 | Pending | TRIAGE | Medium | [LSP] `find-references` returns empty array - investigate if ovim is correctly forwarding LSP request and parsing response |
| OV-00008 | Fixed | HIGH | Medium | [WRAP] Display column vs character column mismatch in wrap scroll offset - Fixed: replaced substring+display_width hack with char_col_to_display_col in update_scroll_offset (src/editor/mod.rs) |
| OV-00009 | Fixed | MEDIUM | Medium | [PERF] ensure_wrap_map rebuilds entire map on every buffer edit - Fixed: use invalidate_line for O(1) single-line updates when only buffer version changed (src/editor/mod.rs:ensure_wrap_map) |
| OV-00010 | Fixed | HIGH | Low | [RENDER] Cursor positioning ignores horizontal_offset in nowrap mode - Fixed: subtract horizontal_offset from display_col in both nowrap branches of set_cursor_position (src/ui/renderer/core.rs) |
| OV-00011 | Fixed | MEDIUM | Medium | [WRAP] gj/gk use character column instead of display column - Fixed: convert char col to display col before wrap map ops, convert back to char col for target position (src/editor/input/normal/pending_commands.rs) |
| OV-00012 | Fixed | MEDIUM | Low | [CMD] Verify :set wrap / :set nowrap command wiring - Already wired up at commands.rs:1787-1802, no code change needed |
| OV-00013 | Fixed | LOW | Medium | [WRAP] split_line_into_rows doesn't handle wide characters at wrap boundaries - Fixed: use UnicodeWidthChar for display width tracking, pad and push wide chars to next row at boundaries (src/ui/renderer/buffer.rs) |
| OV-00014 | Fixed | HIGH | Medium | [WRAP] WrapMap compute_visual_lines disagrees with renderer for wide chars — Fixed: created shared `wrap` module with `compute_wrap_points`/`visual_line_count` that correctly handles wide chars at boundaries. WrapMap now takes line text instead of display width, uses shared wrap computation. [details](issue-docs/OV-00014-wrapmap-wide-char-mismatch.md). (ovim-core/src/wrap.rs, ovim-core/src/editor/wrap_map.rs) |
| OV-00015 | Pending | MEDIUM | Medium | [PERF] Incremental wrap map invalidation only covers cursor line — [details](issue-docs/OV-00015-incremental-invalidation-cursor-only.md). (src/editor/mod.rs:ensure_wrap_map) |
| OV-00016 | Pending | LOW | Medium | [WRAP] No virtcol/curswant tracking for gj/gk — [details](issue-docs/OV-00016-virtcol-curswant.md). (src/editor/input/normal/pending_commands.rs) |
| OV-00017 | Fixed | LOW | Low | [CLEANUP] Remove delegate display_width in editor/mod.rs — Fixed: removed as part of WrapMap API change (now takes line text, not display width). [details](issue-docs/OV-00017-remove-delegate-display-width.md). (src/editor/mod.rs) |
| OV-00018 | Fixed | MEDIUM | High | [UX] Tab completion for file paths in command mode (:e, :tabe, :sp, etc.) — implemented with popup UI, Tab/BackTab cycling, prefix filtering. See OV-00025 for remaining arrow key issue. |
| OV-00019 | Triage | HIGH | N/A | When there is a wrapping line above the cursor in the current buffer and the textwidth option is set, there is a miscalculation causing the vertical cursor position to be incorrect (cursor is below input point). There seems to be other related bugs, so investigation so required. |
| OV-00020 | Fixed | LOW | Low | [CLEANUP] Dead `sign_width > 0` branch in gutter layout — always-true conditional with unreachable else. Fixed: removed dead branch, added SIGN_WIDTH/GUTTER_SPACING constants. |
| OV-00021 | Fixed | LOW | Low | [CLEANUP] `render_hover_window` takes 10 parameters — collapsed layout+viewport into `OverlayContext`. Also applied to `render_completion_menu` and `set_cursor_position`. |
| OV-00022 | Fixed | MEDIUM | Medium | [CLEANUP] `render_to_frame` is 218-line god method — decomposed into `clear_frame`, `compute_frame_layout`, `render_buffer_area`, `render_status_area`, `render_overlays` with `FrameAreas` struct. |
| OV-00023 | Pending | LOW | Low | [TEST] `bug_reproduction_test` segfaults (SIGSEGV signal 11) on both main and feature branches — likely mlua/LuaJIT FFI issue, pre-existing. |
| OV-00024 | Fixed | MEDIUM | Low | [CMD] Tab completion skips first entry when popup was already visible from typing — first Tab called `select_next()` before `accept()`. Fixed: added `tab_accepted` flag to distinguish typing-triggered popup from Tab-triggered. |
| OV-00025 | Pending | MEDIUM | Low | [CMD] Up/Down arrows in path completion don't update command line text — visual selection changes but command line stays the same, so Enter executes the Tab-selected entry, not the arrow-selected one. (src/editor/input/commands.rs) |
| OV-00026 | Fixed | HIGH | Medium | [VISUAL] V<C-u> and V<C-d> broken — half-page scrolling in visual modes was not implemented. Fixed: added Ctrl-D/Ctrl-U handlers to visual_mode.rs. |
| OV-00027 | Fixed | MEDIUM | Medium | [FILETREE] No scroll offset — file tree had no viewport scrolling, selection could go off-screen in large trees. Fixed: added scroll_offset + ensure_visible() to FileTree. |
| OV-00028 | Fixed | MEDIUM | Low | [FILETREE] Selection not clamped after directory collapse — collapsing a directory above selected item could leave selected_index out of bounds. Fixed: clamp in rebuild_flattened(). |
| OV-00029 | Fixed | LOW | Low | [FILETREE] Refresh loses expansion state — refresh() called open() which reset all expansion. Fixed: collect/restore expanded paths across refresh. |
| OV-00030 | Fixed | LOW | Low | [FILETREE] Dead `<Space>e` toggle code in legacy leader handler — leader.rs maps `<Space>e` to diagnostics, legacy handler was unreachable. Fixed: removed dead code. |
| OV-00031 | Fixed | MEDIUM | Medium | [FILETREE] `-` key was pure toggle, no reveal — pressing `-` didn't reveal current file in tree. Fixed: new semantics with reveal_path() and file_tree_reveal option. |
| OV-00032 | Fixed | LOW | Low | [FILETREE] `h` key only collapsed dirs, no parent navigation — on files or collapsed dirs, `h` did nothing. Fixed: navigate_to_parent() fallback. |
| OV-00033 | Fixed | HIGH | Medium | [NUM] `format_number` produces two's complement for negative hex/bin/oct — Fixed: use unsigned_abs() with sign prefix for hex/bin/oct formatting. |
| OV-00034 | Fixed | HIGH | Medium | [NUM] Hex digit scanning only checks `is_ascii_digit()` — Fixed: detect hex context and use is_ascii_hexdigit() for scanning when cursor is on a-f within 0x-prefixed numbers. |
| OV-00035 | Fixed | HIGH | Medium | [NAV] `sentence_backward` can panic on out-of-bounds col — Fixed: clamp col to chars.len()-1 and handle empty lines gracefully. |
| OV-00036 | Won't Fix | MEDIUM | Low | [NUM] `g Ctrl-A` first line gets zero delta — Investigation shows ovim matches Vim behavior: first line +0, second +1, etc. Existing tests confirm this. Not a bug. |
| OV-00037 | Fixed | MEDIUM | Low | [NUM] Ctrl-A/X searches backward for numbers, Vim only searches forward — Fixed: removed backward search branch; now searches forward-only when cursor is not on a digit. |
| OV-00038 | Fixed | MEDIUM | Low | [PASTE] Line paste doesn't reposition cursor to first non-blank — Fixed: after linewise p/P, cursor now moves to first non-blank character of the pasted line. |
| OV-00039 | Fixed | MEDIUM | Medium | [ENCODING] `reload_if_changed` ignores file encoding — Fixed: both reload_if_changed and reload_from_disk now use FileEncoding::detect + decode instead of hardcoded UTF-8. |
| OV-00040 | Fixed | MEDIUM | Low | [SEARCH] `search_next` forward wraps to col 0 of same line — Fixed: when cursor_col+1 >= line_len, now advances to next line (modulo line count) instead of staying on same line. |
| OV-00041 | Fixed | MEDIUM | Medium | [UNDO] Visual mode `~` creates N separate undo entries — Fixed: all per-line changes collected into a single outer composite before add_change. |
| OV-00042 | Fixed | MEDIUM | Medium | [UNDO] Visual block `r` creates N separate undo entries — Fixed: same pattern as OV-00041, collected into single composite. |
| OV-00043 | Fixed | LOW | Low | [WINDOW] Directional focus `|| true` makes overlap preference a no-op — Fixed: removed || true hack; direction-only filter with min_by_key(distance) for selection. |
| OV-00044 | Fixed | MEDIUM | Low | [LSP] No Content-Length validation before allocation in LSP reader — Fixed: bounds check against MAX_MESSAGE_SIZE before allocation. |
| OV-00045 | Won't Fix | LOW | Medium | [SEARCH] Backward search at col=0 produces empty search range — Investigation shows find_backward correctly falls through to previous lines when from_col=0. Not a bug in practice. |
| OV-00046 | Pending | HIGH | High | [ARCH] `commands.rs` is 2K+ line monolith with duplicated handlers — `:bn`/`:bnext` handled in both top-level match AND else-if chain; `:set` has 400 lines of near-identical arms. Needs command table or macro. (src/commands.rs) |
| OV-00047 | Pending | MEDIUM | High | [ARCH] `lsp/server.rs` has 24 AtomicBool fields + 290 lines of capability boilerplate — adding a capability requires changes in 4 places. File is 1900+ lines. Replace with bitflags. (src/lsp/server.rs) |
| OV-00048 | Fixed | MEDIUM | Medium | [RENDER] ANSI escape codes in buffer content interpreted by terminal — control chars (0x00-0x1F excl tab/newline, and 0x7F) now rendered as caret notation (e.g. `^[` for ESC, `^@` for NUL) with SpecialKey highlight group. Buffer stores original bytes untouched for round-trip fidelity. |
| OV-00049 | Pending | LOW | Low | [WINDOW] Window focus overlap preference is dead code — `focus_left/right/up/down` originally had overlap preference checks (`vertical_overlap > 0`) but `|| true` made them no-ops. Now removed. To restore overlap preference, `focus_directional` sort should weight overlapping candidates higher (e.g., multi-key sort: overlap then distance). |
| OV-00050 | Fixed | HIGH | Medium | [RENDER] `slice_horizontal_viewport` uses char count instead of display width — Fixed: rewrote to walk chars by display width, made h_offset display-column-based throughout (ensure_cursor_visible_horizontal, scroll-to-edge functions, shift_highlights_for_viewport). (ovim/src/ui/renderer/buffer.rs, ovim-core/src/editor/mod.rs, window_viewport.rs) |
| OV-00051 | Fixed | HIGH | Medium | [RENDER] Visual selection and diagnostic underlines misaligned on lines with tabs — Fixed: added char_mapping to expand_tabs_with_mapping, remap visual selection/bracket/diagnostic coordinates from original-text to expanded-text space before rendering. Diagnostic UTF-16 offsets properly converted to char indices first. (ovim/src/ui/renderer/buffer.rs, helpers.rs) |
| OV-00052 | Fixed | HIGH | Medium | [PASTE] Linewise paste on last line (no trailing newline) concatenates inline instead of creating new line; characterwise paste on empty line inserts on wrong line — Fixed: detect missing trailing newline and prepend \n for linewise paste; clamp col+1 to line content length for characterwise paste. (ovim-core/src/editor/input/helpers.rs:494-537) |
| OV-00053 | Fixed | HIGH | Low | [MOUSE] Mouse movement triggers scroll — `convert_mouse_event_kind` catch-all `_ => ScrollUp` maps crossterm `Moved`, `ScrollLeft`, `ScrollRight` all to `ScrollUp`. With `EnableMouseCapture` active, every mouse move scrolls viewport up 3 lines. Fixed: added `Moved`, `ScrollLeft`, `ScrollRight` variants to `MouseEventKind` enum, mapped them properly in conversion, ignored in dispatcher. (ovim/src/key_convert.rs:61-71, ovim-core/src/key.rs:60-69) |
| OV-00054 | Pending | TRIAGE | Medium | [LSP] TypeScript diagnostics not appearing for undefined variables — goto-definition works but `publishDiagnostics` errors don't render. Client capabilities don't explicitly advertise `publish_diagnostics` support (`..Default::default()` leaves it as `None`). Some LSP servers (typescript-language-server) may require this. Needs investigation: check if tsserver actually sends diagnostics and they get dropped, or if it never sends them. (ovim-core/src/lsp/server.rs:733-746) |
| OV-00055 | Pending | LOW | Low | [DEAD CODE] `Change::join_lines()` constructor has zero callers — superseded by `RepeatAction::JoinLines` + `Buffer::join_lines()`. The constructor (ovim-core/src/change.rs:269-285) and its match arms in `repeat()` and `undo_redo()` are dead code. (ovim-core/src/change.rs:269) |
| OV-00056 | Pending | MEDIUM | Low | [PANIC] `wrap_map.as_mut().unwrap()` can panic if wrap_map is None — two unwrap calls on Option in `ensure_wrap_map`. If wrap_map initialization fails or is called before setup, these panic in production. Should use `if let Some(map)` or initialize defensively. (ovim-core/src/editor/mod.rs:714,720) |
| OV-00057 | Pending | MEDIUM | Low | [LOGIC] Inconsistent cursor clamping between `validate_cursor_position()` and `clamp_cursor_col()` — `validate_cursor_position()` uses `col >= line_len` (clamps col 0 on 1-char line), while `clamp_cursor_col()` uses `col > 0 && col >= line_len` (preserves col 0). Both use grapheme_count. The `col > 0` guard in `clamp_cursor_col` is intentional (col 0 is always valid), so `validate_cursor_position` has the bug: it would clamp col 0 to col 0 on a 1-char line (no-op in practice, but semantically wrong — it enters the branch unnecessarily). (ovim-core/src/buffer/mod.rs:241, ovim-core/src/buffer/text_ops.rs:307) |
| OV-00058 | Pending | HIGH | Medium | [FEATURE] Operator/motion gap: `b`, `e`, `B`, `E`, `h`, `0`, `^`, `W` not available with d/c/y operators — operators.rs only handles `w`, `$`, `j`, `k`, `{`, `}`, `%`, `l` as motions. Missing: `db` (delete backward word), `de`/`dE` (delete to end of word), `dB` (delete backward WORD), `d0` (delete to start of line), `d^` (delete to first non-blank), `dh` (delete char left), `dW` (delete WORD forward). These are fundamental Vim motions. (ovim-core/src/editor/input/normal/operators.rs) |
| OV-00059 | Pending | LOW | Low | [TEST] No unit tests for Buffer methods `toggle_char_at_cursor()`, `indent_lines_at()`, `dedent_lines_at()`, `clamp_cursor_col()` — these were extracted from Editor-level code and have integration test coverage via repeat_action_test and cursor_clamping_test, but no direct Buffer-level unit tests. (ovim-core/src/buffer/text_ops.rs) |
| OV-00060 | Pending | LOW | Low | [TEST] Missing `~` toggle case on empty line test — `toggle_char_at_cursor()` returns false on empty line but no test verifies this edge case. Also missing: undo/redo cycle tests for each RepeatAction variant (indent, dedent, toggle case, join). (ovim-core/src/buffer/text_ops.rs:325) |
| OV-00061 | Pending | MEDIUM | High | [ARCH] 11 files exceed 1.5k lines (CLAUDE.md says refactor at 3k, split before adding) — editor/mod.rs (1952), lsp/server.rs (1943), motions.rs (1898), commands.rs (1840), lsp/requests.rs (1815), change.rs (1769), subcommands.rs (1766), renderer/buffer.rs (1743), input/commands.rs (1636), helpers.rs (1611), operators.rs (1523). Several are approaching or past the 2k guideline. (multiple files) |
| OV-00062 | Pending | MEDIUM | High | [ARCH] Dual undo system mid-migration — Pattern A (`Change::insert/delete` + `add_change()`, 73 call sites) coexists with Pattern B (`record()` + `push_recorded_undo()` + `set_repeat_action()`, ~47 call sites). Pattern A is still needed for insert-mode undo grouping. The `Change` enum has dead variants (like `JoinLines`) from the migration. No clear boundary between which operations use which pattern. (ovim-core/src/change.rs, ovim-core/src/editor/change_tracking.rs) |
| OV-00063 | Pending | HIGH | Medium | [UNDO] `pop_last_change` pops unrelated undo entry when delete phase produces no edits — `C` at end of line or `s` on empty line: the delete phase via `record()` produces no edits, so no `Change::Recorded` is pushed. The second `pop_last_change()` in exit_insert_mode grabs an unrelated earlier change, corrupting undo history. (ovim-core/src/editor/input/insert_mode.rs:86-89) |
| OV-00064 | Pending | MEDIUM | Medium | [UNDO] save_point corrupted by pop_last_change — After saving, `cc`/`C`/`s` causes pop_last_change to reduce stack size, then push_change increases it, making save_point index wrong. Buffer may appear unmodified when it has changes, or modified when clean. (ovim-core/src/change.rs:1196-1204, insert_mode.rs:86-107) |
| OV-00065 | Pending | LOW | Low | [UNDO] Missing validate_cursor_position in semantic change undo variants — `ChangeTextObject`, `ChangeWord`, `ReplaceMode`, `ChangeSearchMatch` undo methods don't validate cursor after restoring position, while their redo counterparts do. Could leave cursor on invalid position after undo. (ovim-core/src/change.rs:484-552) |
| OV-00066 | Pending | MEDIUM | Medium | [INDENT] expandtab setting completely ignored by indent/dedent — All indent operations unconditionally insert spaces. When `expandtab=false`, should insert tab characters. (ovim-core/src/buffer/text_ops.rs:316-323) |
| OV-00067 | Pending | MEDIUM | Low | [INDENT] shiftwidth setting ignored — all indent operations use `tabstop` instead of `shiftwidth` for indent size. In Vim, `>>` uses shiftwidth, not tabstop. (ovim-core/src/editor/input/normal/operators.rs) |
| OV-00068 | Pending | LOW | Low | [INDENT] `>>` indents empty lines — Vim skips indentation of empty/whitespace-only lines. ovim indents them. (ovim-core/src/buffer/text_ops.rs:316-323) |
| OV-00069 | Pending | MEDIUM | Low | [INDENT] Cursor positioned on LAST indented line instead of CURRENT line after `>>` — multi-line `>>` should leave cursor on the first affected line at the first non-blank, not the last line. (ovim-core/src/editor/input/helpers.rs:938-972) |
| OV-00070 | Pending | LOW | Low | [INDENT] Cursor positioned at column tab_width instead of first non-blank after `>>` — After indenting, cursor should be at first non-blank character, not at a fixed column offset. (ovim-core/src/repeat_action.rs:93) |
| OV-00071 | Pending | LOW | Low | [INDENT] Cursor not moved to first non-blank after `<<` — After dedenting, Vim moves cursor to first non-blank. ovim just clamps cursor column. (ovim-core/src/repeat_action.rs:103) |
| OV-00072 | Pending | MEDIUM | Medium | [INDENT] Visual mode `=` (auto-indent) has no undo support — Uses non-tracking `auto_indent_lines`, so changes cannot be undone. (ovim-core/src/editor/input/visual_mode.rs:747-758) |
| OV-00073 | Pending | LOW | Low | [INDENT] auto_indent_lines (non-tracking) uses byte length for leading_len — Should use char count for consistent behavior with multi-byte characters. (ovim-core/src/editor/input/helpers.rs:1494-1503) |
| OV-00074 | Pending | LOW | Low | [INDENT] Ctrl-T always inserts spaces, ignoring expandtab — Insert-mode indent should insert tab characters when expandtab is off. (ovim-core/src/editor/input/helpers.rs:329-353) |
| OV-00075 | Pending | HIGH | Medium | [SCROLL] `viewport_command_active` never reset, permanently disabling scrolloff — After any `zt`/`zz`/`zb`, the flag stays true permanently, making `update_scroll_offset()` use a no-scrolloff code path for the rest of the session. (ovim-core/src/editor/mod.rs:794, window_viewport.rs:234,261,288) |
| OV-00076 | Pending | HIGH | Medium | [SCROLL] Ctrl-e/Ctrl-y don't update buffer cursor, scroll overridden by update_scroll_offset — scroll_viewport_down/up only modify window cursor, not buffer cursor. Post-key update_scroll_offset reads the unmoved buffer cursor and pulls viewport back, negating the scroll. (ovim-core/src/editor/window_viewport.rs:193-209, input/mod.rs:132-133) |
| OV-00077 | Pending | MEDIUM | Medium | [SCROLL] update_scroll_offset uses editor-level viewport_height, wrong for split windows — In horizontal splits, each window's height is a fraction of the total, but scrolloff calculations use the full content area height. (ovim-core/src/editor/mod.rs:755) |
| OV-00078 | Pending | MEDIUM | Low | [SCROLL] sidescrolloff not clamped, causing oscillation with narrow viewports — Unlike vertical scrolloff, horizontal sidescrolloff is never clamped. When `sidescrolloff * 2 >= visible_width`, left_bound and right_bound cross over, creating impossible constraints. (ovim-core/src/editor/window.rs:141-142) |
| OV-00079 | Pending | MEDIUM | Low | [SCROLL] undo/redo don't reset viewport_command_active — Compounds OV-00075: after `zt` + undo, scrolloff remains disabled. (ovim-core/src/editor/change_tracking.rs:39-52) |
| OV-00080 | Pending | HIGH | Low | [MOTION] `G` with count not clamped to buffer bounds — `999G` on a 3-line buffer sets cursor to line 998. No clamping to `buffer.line_count() - 1`. (ovim-core/src/editor/input/normal/motions_input.rs:141-156) |
| OV-00081 | Pending | HIGH | Low | [MOTION] `gg` with count not clamped to buffer bounds — Same as OV-00080 but for `gg`. (ovim-core/src/editor/input/normal/pending_commands.rs:36-46) |
| OV-00082 | Pending | MEDIUM | Medium | [MOTION] `b` from col 0 goes to end of previous line instead of start of last word — Should land on start of last word on previous line, but lands on last character. (ovim-core/src/editor/motions.rs:224-234) |
| OV-00083 | Pending | MEDIUM | Low | [MOTION] `$` ignores count — In Vim, `2$` goes to end of next line (count-1 lines down). ovim ignores count and stays on current line. (ovim-core/src/editor/input/normal/motions_input.rs:58-68) |
| OV-00084 | Pending | LOW | Low | [MOTION] `_` (underscore) ignores count — In Vim, `3_` goes to first non-blank 2 lines down. ovim treats `_` identically to `^`. (ovim-core/src/editor/input/normal/motions_input.rs:75-78, motions.rs:833-836) |
| OV-00085 | Pending | LOW | Low | [MOTION] `+` uses rope.len_lines() instead of buffer.line_count() — Includes ropey's phantom empty line after trailing newline, allowing cursor on non-existent line. (ovim-core/src/editor/motions.rs:838-847) |
| OV-00086 | Pending | MEDIUM | Medium | [MOTION] `%` doesn't search forward for bracket when cursor is not on one — Vim searches forward on current line for nearest bracket, then jumps to its match. ovim only works if cursor is already on a bracket. Also matches `<`/`>` which Vim doesn't. (ovim-core/src/editor/motions.rs:734-746) |
| OV-00087 | Pending | MEDIUM | Low | [MOTION] `t` returns success when cursor doesn't move, affects `dt` — When target is adjacent (i-1 == col), returns true without movement, causing operator to apply over zero-width range that gets expanded by inclusive logic. (ovim-core/src/editor/motions.rs:653-665) |
| OV-00088 | Pending | MEDIUM | Low | [MOTION] `]}` and `])` include character under cursor in search — Forward unmatched brace/paren search starts at cursor position, so if cursor is on `}`, it matches immediately. Should skip cursor char. (ovim-core/src/editor/motions.rs:1575-1594) |
| OV-00089 | Pending | MEDIUM | Medium | [MOTION] `ge` moves forward when cursor is in middle of word — Goes back to start of current word, then forward to its end. Can produce net forward movement on what should be a backward motion. (ovim-core/src/editor/motions.rs:470-573) |
| OV-00090 | Pending | MEDIUM | Low | [PASTE] p/P with count not implemented — Count is ignored for paste commands. Vim pastes the text N times. (ovim-core/src/editor/input/normal/editing_commands.rs:51-60) |
| OV-00091 | Pending | MEDIUM | Low | [PASTE] P (paste before) charwise cursor off by one — After charwise paste before cursor, cursor is positioned one character too far right. (ovim-core/src/editor/input/helpers.rs:722-727) |
| OV-00092 | Pending | MEDIUM | Medium | [PASTE] Visual-mode paste doesn't put replaced text into unnamed register — In Vim, visual paste stores the replaced text in the unnamed register so subsequent `p` pastes the old text. ovim doesn't update the register. (ovim-core/src/editor/input/visual_mode.rs:668-688) |
| OV-00093 | Pending | MEDIUM | Medium | [PASTE] Visual-line paste places text one line too low — Deletion and insertion of new text interact to place text one line below where it should go. (ovim-core/src/editor/input/visual_mode.rs:678-687) |
| OV-00094 | Pending | MEDIUM | Medium | [REGISTER] Numbered registers (1-9) lose RegisterType information — `delete_history` is `Vec<String>` not `Vec<RegisterContent>`, so querying digit registers always returns Character type even for linewise deletes. (ovim-core/src/editor/register.rs:93-94, 213) |
| OV-00095 | Pending | LOW | Low | [REGISTER] Named register yank/delete does not update unnamed register — In Vim, `"ayy` also updates the unnamed register. ovim only updates the named register. (ovim-core/src/editor/mod.rs:1441-1493) |
| OV-00096 | Pending | LOW | Low | [REGISTER] Uppercase register append doesn't update RegisterType — Appending to a register with uppercase name (e.g. `"Ayy`) doesn't update the register's type metadata. (ovim-core/src/editor/register.rs:151-158) |
| OV-00097 | Pending | LOW | Low | [PASTE] Linewise paste on empty buffer places cursor on wrong line — Edge case when buffer is empty and linewise paste is performed. (ovim-core/src/editor/input/helpers.rs:473-614) |
| OV-00098 | Pending | LOW | Low | [PASTE] Linewise paste_after on last line strips trailing newline — When pasting after the last line, the trailing newline handling creates incorrect text. (ovim-core/src/editor/input/helpers.rs:473-614) |
| OV-00099 | Pending | HIGH | Medium | [EDIT] `:e!` does not clear undo history on reload — `reload_from_disk()` replaces rope but leaves ChangeManager intact. After reload, `u` applies old undo entries to new content, corrupting buffer. (ovim-core/src/buffer/file_io.rs:477, commands.rs:1274) |
| OV-00100 | Pending | HIGH | Medium | [EDIT] `:e` (bare) does not clear undo history on reload — Same as OV-00099 but for the no-arg `:e` reload path. External file changes + stale undo = corruption. (ovim-core/src/commands.rs:1247) |
| OV-00101 | Pending | HIGH | Low | [EDIT] `:e filename` does not check for unsaved changes — Silently abandons unsaved work when opening a different file. Vim refuses with E37 unless `!` is used. (ovim-core/src/commands.rs:1295-1324) |
| OV-00102 | Pending | MEDIUM | Low | [EDIT] `:e! filename` (force-edit different file) not supported — Only `e!` (exact match) handled. `:e! somefile` gives "Not an editor command". (ovim-core/src/commands.rs:1274, 1295) |
| OV-00103 | Pending | MEDIUM | Low | [EDIT] `reload_from_disk` does not reset fold state — Fold line references invalidated by content change; can cause rendering artifacts. (ovim-core/src/buffer/file_io.rs:477-538) |
| OV-00104 | Pending | MEDIUM | Low | [EDIT] `reload_from_disk` does not increment version counter — LSP hover cache considers stale data valid since buffer_version unchanged. (ovim-core/src/buffer/file_io.rs:477-538) |
| OV-00105 | Pending | MEDIUM | Medium | [EDIT] `reload_from_disk` does not notify LSP of content change — LSP server works with stale content after reload until some other edit triggers sync. Should send didChange or didClose+didOpen. (ovim-core/src/buffer/file_io.rs:477, commands.rs:1259) |
| OV-00106 | Pending | MEDIUM | Low | [EDIT] Cursor clamping in `reload_from_disk` includes newline char — Uses raw `len_chars()` without subtracting newline, placing cursor one position too far right. (ovim-core/src/buffer/file_io.rs:528-534) |
| OV-00107 | Pending | MEDIUM | Medium | [EDIT] `:checktime` reload doesn't reset undo/cursor/folds — `reload_if_changed` has same stale-undo problem as `reload_from_disk`. (ovim-core/src/buffer/file_io.rs:414-465) |
| OV-00108 | Pending | LOW | Low | [EDIT] `reload_from_disk` does not reset git status/blame — Gutter signs show outdated info until something else triggers refresh. (ovim-core/src/buffer/file_io.rs:477-538) |
| OV-00109 | Pending | LOW | Low | [EDIT] `reload_from_disk` does not clear highlight/cache state — `cached_highlights`, `semantic_highlights`, `code_block_cache` stale after reload. (ovim-core/src/buffer/file_io.rs:477-538) |
| OV-00110 | Pending | LOW | Low | [DEAD CODE] Duplicate `:e filename` handler in input/commands.rs — Dead code at input/commands.rs:1503-1511, unreachable because commands.rs handles it first. (ovim-core/src/editor/input/commands.rs:1503) |

## Bugs Filed Against Hyperion (if any)

These may belong in Hyperion's tracker after investigation:

| ID | Status | Priority | Description |
|----|--------|----------|-------------|
| HY-TRIAGE-01 | Triage | UNKNOWN | hover not returning info for method calls - may be unimplemented in hyperion-lsp |
| HY-TRIAGE-02 | Triage | UNKNOWN | find-references returning empty - may be unimplemented in hyperion-lsp |
| HY-TRIAGE-03 | Triage | UNKNOWN | goto-definition fails on DTO type references (AdminSqlRequest) - possibly classpath/dependency resolution |

## Notes

- OV-00001 fixed by adding goto-line parsing to commands.rs; OV-00002 fixed by improving client error handling
- OV-00003 and OV-00004 are MCP vs LSP protocol mismatch - ovim uses MCP, some LSPs don't implement those tools
- HY-TRIAGE items need investigation to determine if they're hyperion-lsp gaps or ovim request/response issues
